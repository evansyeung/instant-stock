export type Product = {
  atcUrl: string,   // Add to cart URL
  brand: string,
  currentPrice?: string | null,
  id: string,
  name: string,
  series: string,
  type: "graphics-card",
  url: string,
}
